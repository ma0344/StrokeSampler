# Issue: プロジェクト構造を把握する

## 概要
現状のソリューション/プロジェクト構造を確認し、鉛筆サンプル生成～PNG書き出し機能を実装するための変更箇所を明確化する。

## 目的（完了条件）
- `StrokeSampler.csproj` と `MainPage.xaml(.cs)` の現状が把握できている。
- 追加が必要な依存関係（Win2D等）と編集対象ファイルが特定できている。

## ロードマップ小項目（割り当て）
- `net9.0-windows...` / `UseUwp=true` のUWPアプリであることを確認する
- 既存UI/ロジックが空であることを前提に設計する

## 作業内容
- ソリューション内のプロジェクトとファイルを列挙する。
- `MainPage` の現状UI/コードの有無を確認する。
- 書き出し先の方針（`FileSavePicker` 等）と必要権限の有無を整理する。

## 影響範囲
- なし（調査のみ）

## メモ
- 本Issueは実装前の前提整理。