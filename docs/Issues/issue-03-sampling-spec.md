# Issue: サンプリング仕様（Pressure/線形状/配置）を確定する

## 概要
サンプル生成のパラメータ（Pressure段階、線形状、配置間隔等）を決め、実装で一貫して使える形にする。

## 目的（完了条件）
- Pressure の段階（`0.2 / 0.5 / 0.8 / 1.0`）が決まっている。
- サンプル線の形状（例: 水平直線）と、重ならない配置（spacing）が決まっている。
- 出力画像サイズ/背景（素材用=透過、確認用=白）が決まっている（初期値がある）。

## ロードマップ小項目（割り当て）
- Pressure を `0.2 / 0.5 / 0.8 / 1.0` の4段階に固定する
- Stroke形状を直線（水平線）に統一する
- 縦方向に重ならないよう `spacing` を付ける
- 太さ/色は `InkToolbar` の選択値を使用する（取得できない環境では固定値にフォールバックする）

## 作業内容
- 既定値（定数/設定値）の一覧を作る。
- 将来UIで変更する可能性がある項目を整理する（初期版は固定でも可）。

## 影響範囲
- 設計のみ（必要に応じて `docs` 更新）

## メモ
- サンプルの再現性のため、初期版は乱数や入力依存を避ける。